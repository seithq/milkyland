<% content_for(:title) { t("pages.supply_operations") } %>

<% content_for :breadcrumb do %>
  <%= render "layouts/breadcrumb", {
    nav_links: [
      NavLink.new(path: supply_operations_path, title: t("pages.supply_operations"))
    ]
  } %>
<% end %>

<%= render "form", waybill: @waybill %>
