<% content_for(:title) { t("pages.supply_operation", id: @waybill.id) } %>

<% content_for :breadcrumb do %>
  <%= render "layouts/breadcrumb", {
    nav_links: [
      NavLink.new(path: supply_operations_path, title: t("pages.supply_operations")),
      NavLink.new(path: edit_supply_operation_path(@waybill), title: t("pages.supply_operation", id: @waybill.id))
    ]
  } %>
<% end %>

<%= render "form", leftover: @leftover %>