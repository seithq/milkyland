<%= turbo_frame_tag dom_id(@packing, :products) do %>
  <div class="flow-root">
    <div class="-my-2 overflow-x-auto">
      <div class="inline-block min-w-full py-2 align-middle px-2">
        <table class="min-w-full divide-y divide-gray-300">
          <thead>
          <tr>
            <th scope="col" class="py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-gray-900 sm:pl-0"><%= t("tables.cols.product") %></th>
            <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900"><%= t("tables.cols.comment") %></th>
            <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900"><%= t("tables.cols.start_time") %></th>
            <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900"><%= t("tables.cols.end_time") %></th>
            <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900"><%= t("tables.cols.count") %></th>
            <% if @packing.in_progress? %>
              <th scope="col" class="relative py-3.5 pl-3 pr-4 sm:pr-0">
                <span class="sr-only"><%= t("actions.edit_record") %></span>
              </th>
            <% end %>
          </tr>
          </thead>
          <tbody class="divide-y divide-gray-200">
          <%= render @packaged_products %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
<% end %>