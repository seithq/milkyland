<% content_for(:title) { t("pages.home") } %>

<% content_for :breadcrumb do %>
  <%= render "layouts/breadcrumb", {
    nav_links: [
      NavLink.new(path: regions_path, title: t("pages.home")),
    ]
  } %>
<% end %>