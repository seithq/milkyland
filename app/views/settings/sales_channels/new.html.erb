<% content_for(:title) { t("pages.channels") } %>

<% content_for :breadcrumb do %>
  <%= render "layouts/breadcrumb", {
    nav_links: [
      NavLink.new(path: sales_channels_path, title: t("pages.channels"))
    ]
  } %>
<% end %>

<%= render "form", sales_channel: @sales_channel %>
