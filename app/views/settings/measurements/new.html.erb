<% content_for(:title) { t("pages.measurements") } %>

<% content_for :breadcrumb do %>
  <%= render "layouts/breadcrumb", {
    nav_links: [
      NavLink.new(path: measurements_path, title: t("pages.measurements"))
    ]
  } %>
<% end %>

<%= render "form", measurement: @measurement %>
