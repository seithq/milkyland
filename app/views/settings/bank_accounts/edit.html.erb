<% content_for(:title) { @bank_account.name } %>

<% content_for :breadcrumb do %>
  <%= render "layouts/breadcrumb", {
    nav_links: [
      NavLink.new(path: bank_accounts_path, title: t("pages.bank_accounts")),
      NavLink.new(path: edit_bank_account_path(@bank_account), title: @bank_account.name),
    ]
  } %>
<% end %>

<%= render "form", bank_account: @bank_account %>
