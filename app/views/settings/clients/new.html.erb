<% content_for(:title) { t("pages.clients") } %>

<% content_for :breadcrumb do %>
  <%= render "layouts/breadcrumb", {
    nav_links: [
      NavLink.new(path: clients_path, title: t("pages.clients"))
    ]
  } %>
<% end %>

<%= render "form", client: @client %>
