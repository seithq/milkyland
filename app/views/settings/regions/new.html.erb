<% content_for(:title) { t("pages.regions") } %>

<% content_for :breadcrumb do %>
  <%= render "layouts/breadcrumb", {
    nav_links: [
      NavLink.new(path: regions_path, title: t("pages.regions"))
    ]
  } %>
<% end %>

<%= render "form", region: @region %>
