<% tab_links = [
  NavLink.new(path: storages_path, title: t("pages.storages"), is_active: true),
  NavLink.new(path: waybills_path, title: t("pages.waybills"), is_active: false),
  NavLink.new(path: boxes_path, title: t("pages.boxes"), is_active: false),
  NavLink.new(path: root_path, title: t("pages.pallets"), is_active: false)
]
%>

<div>
  <div class="sm:hidden">
    <%= mobile_tab_select tab_links %>
  </div>
  <div class="hidden sm:block">
    <div class="border-b border-gray-200">
      <nav class="-mb-px flex space-x-8" aria-label="Tabs">
        <% tab_links.each do |link| %>
          <%= link_to link.title, link.path, class: "tab-link #{ "tab-link-active" if link.is_active }" %>
        <% end %>
      </nav>
    </div>
  </div>
</div>